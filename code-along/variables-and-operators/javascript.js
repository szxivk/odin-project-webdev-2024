let name = "Sz";
let message = "Hello!";

console.log(name);
console.log(message);

let age = "24"; // initialization
console.log(age);

age = 25; // we only need to declare the variable once
console.log(age);

const pi = 3.14;
console.log(pi);

pi = 22;
console.log(pi);

/*
-> two ways to declare a variable

let: which we can re-assign.
const: which we can’t re-assign and will throw an error if we try.

*/